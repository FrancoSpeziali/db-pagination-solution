{
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.13",
    "prettier": "^2.4.1"
  },
  "scripts": {
    "setup": "npm i && cd client && npm i",
    "build": "cd client && npm run build"
  }
}
